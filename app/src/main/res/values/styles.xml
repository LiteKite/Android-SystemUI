<resources>

    <!-- Base application theme. -->
    <style name="Theme.SystemUI" parent="@android:style/Theme.DeviceDefault">
        <!-- Needed for MediaRoute chooser dialog -->
        <item name="*android:isLightTheme">false</item>
    </style>

</resources>